---
import BaseLayout from '../../../layouts/BaseLayout.astro';

// Required for dynamic routes - returns empty array for now
export function getStaticPaths() {
  // Will be populated with city data later
  return [];
}

const { city } = Astro.params;
---

<BaseLayout title="City Clinics">
  <h1>Clinics in {city}</h1>
  <p>City listing page - will show all clinics in this city</p>
  <p>This is a placeholder template for future city pages.</p>
</BaseLayout>
