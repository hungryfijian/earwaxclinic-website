---
import BaseLayout from '../../../layouts/BaseLayout.astro';

// Required for dynamic routes - returns empty array for now
export function getStaticPaths() {
  // Will be populated with real clinic data later
  return [];
}

const { city, clinic } = Astro.params;
---

<BaseLayout title="Clinic Template">
  <h1>Clinic page for: {clinic} in {city}</h1>
  <p>Content will be dynamically generated</p>
  <p>This is a placeholder template for future clinic pages.</p>
</BaseLayout>
